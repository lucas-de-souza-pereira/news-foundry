export const API_ROUTES = {
  AUTH: {
    LOGIN: "/api/auth/login",
  },
  CHAT: {
    START: "/api/chat/conversations",
  }
} as const;


export const APP_ROUTES = {
  HOME: "/home",
  LOGIN: "/login",
} as const;